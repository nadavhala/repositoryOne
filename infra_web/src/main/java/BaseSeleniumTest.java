import logger.ConsoleLogger;
//import org.openqa.selenium.WebDriver;

public class BaseSeleniumTest {

	//public WebDriver driver;
/*
	@BeforeSuite
	protected void beforeSuiteInits(String xmlRunner, String testPropertiesFileName){ //the xmlRunner is 99% the testng.xml of the suite
		ConsoleLogger.initializeLogConfiguration();


		baseSelenium.initSuite(testPropertiesFileName);

		if (DriverPropertiesInitializer.isEnableKDT()) {
			baseSelenium.setEnv();
		}
	}
*/
}
